language: python

python:
  - 3.7

install:
  - pip install -r requirements.txt
  - pip install coveralls
  - pip install coverage

script:
  - pytest tests/tests.py --cov=src

after_success:
  - coveralls
